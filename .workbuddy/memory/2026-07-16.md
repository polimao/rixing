# 2026-07-16 工作日志

## 推广文案产出（用户目标：为「日行/RiXing」增加用户）
- 产出文件：`docs/promotion-copy.md` —— 一套可直接发布的推广文案，覆盖四个渠道：
  - 少数派（sspai）体验长文（含两种标题 + 正文草稿）
  - V2EX 自荐帖（`分享创造`/`macOS` 节点）
  - Product Hunt 英文发布文案（Name/Tagline/Maker 留言/Topics）
  - Hacker News Show HN（英文标题 + 首段 + 讨论引导）
  - 附：通用素材（一句话定位、数据钩子、链接）与发布前检查清单
- 统一卖点口径：本地 AI、零订阅、零云端、不到 6MB、隐私优先。
- 关键提示已写进文案：当前无 Apple 签名公证，首次打开需右键打开/`xattr -cr`；建议推广前先解决签名以保住转化漏斗顶端。

## 落地页优化（docs/index.html + docs/i18n.js）
- 用户指出落地页已存在（docs/index.html），要求优化而非新建。
- 核心问题：落地页完全漏掉了最强卖点「本地 AI 音乐」和「本地翻译」，功能卡片只讲旧定位（待办/日历/分屏）。
- 优化内容：
  1. 在 Hero 后新增「本地 AI · 免密钥零订阅」强调区块（ai_title/ai1~3，zh+en 双语文案）。
  2. 功能卡片补 f7 本地 AI 音乐、f8 本地翻译（共 8 张卡）。
  3. 截图区后新增 FAQ 区块（收费/Intel 支持/数据存储/首次打开，4 条）。
  4. 下载链接由写死的 dmg 直链改为指向 `releases/latest` 页面，避免资产名不匹配导致 404（i18n.js DOWNLOAD 中/英均改；HTML 初始 href 同步）。
  5. 全语言「安装包 ~4 MB」统一改为「~5 MB」。
- 坑：对 i18n.js 同文件并行多个 Edit 触发竞态（报 "file modified since read"，中文 key 丢失、体积数字只改了一半）。**改为串行逐条 Edit 后全部成功**。教训：同文件多 Edit 务必串行。
- 本地预览：已用 `python3 -m http.server 8137 --directory docs` 起服务，present_files 打开 http://localhost:8137/index.html。
