# 2026-07-15 工作日志

- 更新 README（中文/英文）：新增「防休眠」「开机自启动（英文版）」功能说明，修正 `.dmg` 大小徽章为 ~5.3 MB。
- 重新生成所有 README 截图（todos-light/dark、calendar-light/dark、settings-light/dark）。使用 Playwright + Chromium 无头渲染：复制 `src/` 到临时目录，注入 `window.__TAURI__` mock 提供样例待办与设置，渲染真实 UI 后按内容高度自动裁剪截图。
- 修正 `src/settings.html` 中关于页版本 fallback：`v1.0.0` → `v1.1.1`。
- 提交并推送改动到 origin main。
- 确认 v1.1.1 GitHub Release 已发布，资产为 `RiXing_1.1.1_aarch64.dmg` / `.app.tar.gz`（英文前缀，避免 GitHub 吞前缀）。校验 DMG 有效，签名为 ad-hoc。
- 补做遗漏的 `docs/images/hero.png`：源文件为 `docs/_hero.html`（合成图，引用 todos-light / calendar-light / settings-dark 三张截图 + 伪菜单栏）。直接用 Playwright 渲染该 HTML（无需 mock，静态合成）重出 1280x720 @2x，并把示例日期从 6月23日 改为 7月15日。提交并推送。hero 仅为 README 配图，不在 release 资产内。
- 重新定位 README（中/英）以提升吸引力：新 slogan 强调「菜单栏效率台 + 本地 AI + 隐私」；新增「为什么选日行 / Why RiXing」四支柱（隐私优先、真 AI 免密钥、轻巧原生、一个 App 多面手）；功能顺序改为先讲两个本地 AI 钩子（轻听/MRT2、HY-MT 翻译）再讲待办/番茄钟/农历日历、纪念日倒计时、分屏、防休眠等；日历特性补上「纪念日倒计时」(此前只写了农历和节假日)；中英文 README 对齐（统一徽章、新增「为什么这么小」说明、统一顺序）。品牌名 rixing 日行 不变。已提交并推送 (d89e64d)。
