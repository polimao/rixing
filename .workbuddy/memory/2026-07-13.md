## 2026-07-13

- **UI 调整**：移除待办列表两侧边距——`.todo-container` 改为 `padding: 15px 0`，`.todo-item` 改为 `padding: 0 8px`，`.group-header` 改为 `padding: 12px 12px`，让待办项几乎延伸到窗边但保留 8px 微量呼吸空间；textarea 同步 `width: calc(100% + 8px)` / `margin-left: -8px` / `padding-left: 16px`。
- **删除圆角**：移除待办页面输入框和卡片的所有 `border-radius`（`.todo-container` 10px、`.todo-group` 8px、`.group-header` 顶部、`.todo-item:last-child` 底部、`textarea.text` 6px 及 `:focus` 8px、`.todo-item` 的 `border-radius:0` 声明），改为直角。
