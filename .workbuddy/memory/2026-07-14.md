## 2026-07-14

- **待办页面去边框**：按用户要求，待办页面中除 `.group-header` 的 `border-bottom`（分组头分隔线，含暗色主题 `rgba(255,255,255,0.05)` 覆盖）外，取消所有 border——`.todo-container`、`.todo-group`、`.todo-item`（及冗余的 `:last-child` 规则）、`textarea.text` 基础态（`transparent`→`none`）、聚焦态（`border: 1px solid #409eff`→删除，仅留 box-shadow 蓝色光晕作焦点提示）、`.custom-select` 徽章边框；并移除聚焦态残留的 `border-radius:5px`（呼应之前"删输入框圆角"）。暗色主题中 `.todo-item` 的失效 `border-bottom-color` 覆盖一并清理。
- **打包发布 v1.1.1 release**：`cargo tauri build` 在 DMG 阶段因 create-dmg 的 AppleScript（Finder 权限 -10004）失败，改用 `bundle_dmg.sh --skip-jenkins` 手动生成 dmg 绕过；版本从 1.1.0 升到 1.1.1（远程已有 v1.1.0，不覆盖）；提交 style.css + tauri.conf.json，打 tag v1.1.1 并推送到 github.com/polimao/rixing；GitHub Release 资产须用英文前缀 `RiXing_1.1.1_aarch64.dmg`/`.app.tar.gz`（中文"日行"前缀会被吞）。产物 dmg 校验通过、签名为 ad-hoc（无证书）。详见 MEMORY.md 的"发布 release 的坑"。
