# 2026-07-27

## 日历「休」日样式改为红底白字
- 修改 `src/calendar.js`：当日期带有「休」badge 时，给 `.day` 额外添加 `.holiday-rest` class。
- 修改 `src/calendar.html`：新增 `.holiday-rest` 样式，使其与 `.today` 保持一致（红底 `#cc252c`、白字、粉色农历、红色投影），并覆盖暗色模式、跨月褪色、纪念日标记等边界情况。
- 「休」标识文字保留，仅背景/文字色与今日一致。

## 展开日历标题次月改为阿拉伯数字
- 修改 `src/calendar.js` 的 `monthYearLabel()`：展开状态下，次月使用 `month: 'short'` 格式化，中文从「十月」变成「10月」，与「2026年9月」的月份数字风格一致。
- 非展开状态及跨年份场景保持不变。

## 日历「休」日连体块（同行为一块）
- 新增 `src/calendar.js` 的 `applyRestBlockCorners()`：按 7 列一行扫描连续 `.holiday-rest` 单元格，段首加 `rest-first`（去右圆角）、段中加 `rest-mid`（四角平 + 去中间投影）、段尾加 `rest-last`（去左圆角）；段长=1 不加 class。在 `renderCalendar()` padding 循环后调用。
- `src/calendar.html` 在 `.day.holiday-rest` 样式区（亮/暗各一套）追加 `.rest-mid/.rest-first/.rest-last` 三条圆角规则。
- 仅按行连接，跨周不连；颜色/白字/「休」标识不变。
