# 2026-07-27

## 日历「休」日样式改为红底白字
- 修改 `src/calendar.js`：当日期带有「休」badge 时，给 `.day` 额外添加 `.holiday-rest` class。
- 修改 `src/calendar.html`：新增 `.holiday-rest` 样式，使其与 `.today` 保持一致（红底 `#cc252c`、白字、粉色农历、红色投影），并覆盖暗色模式、跨月褪色、纪念日标记等边界情况。
- 「休」标识文字保留，仅背景/文字色与今日一致。

## 展开日历标题次月改为阿拉伯数字
- 修改 `src/calendar.js` 的 `monthYearLabel()`：展开状态下，次月使用 `month: 'short'` 格式化，中文从「十月」变成「10月」，与「2026年9月」的月份数字风格一致。
- 非展开状态及跨年份场景保持不变。

## 日历「休」日连体块（同行为一块）
- 新增 `src/calendar.js` 的 `applyRestBlockCorners()`：按 7 列一行扫描连续 `.holiday-rest` 单元格，段首加 `rest-first`（去右圆角）、段中加 `rest-mid`（四角平 + 去中间投影）、段尾加 `rest-last`（去左圆角）；段长=1 不加 class。在 `renderCalendar()` padding 循环后调用。
- `src/calendar.html` 在 `.day.holiday-rest` 样式区（亮/暗各一套）追加 `.rest-mid/.rest-first/.rest-last` 三条圆角规则。
- 仅按行连接，跨周不连；颜色/白字/「休」标识不变。

## 提交记录
- 按功能拆分为两个 commit（未 push）：
  - `53f6663` 翻译功能：重构翻译面板卡片布局与结果区显隐，整理首页头部按钮（i18n.js / index.html / settings.html / translate.css / translate.js / memory/2026-07-18.md）
  - `3fc5d50` 日历：休日红底白字、次月阿拉伯数字、同行休日连体块（calendar.html / calendar.js / memory/2026-07-27.md）
- 暂存区原本还混着之前已 staged 的翻译重构文件，已按要求拆分提交。

## 发布 v1.1.4
- 标题格式改动（展开标题 `2026年7·8月`）+ 版本号升 1.1.3 → 1.1.4，commit `89d72b8` 已 push，tag `v1.1.4` 已 push。
- `cargo tauri build` 本次 DMG 阶段**未假失败**，直接 Finished 2 bundles（当前环境 Finder 自动化权限可用）。dmg 校验 VALID。
- 英文前缀资产 `RiXing_1.1.4_aarch64.dmg` / `.app.tar.gz` 已上传；Release https://github.com/zlimao/rixing/releases/tag/v1.1.4 （ad-hoc 签名）。

## 设置窗口标题左移
- 修改 `src/settings.html`：`.window-drag-bar` 改为 `justify-content: flex-start` + `padding-left: 100px`（与内容区左边缘对齐：侧边栏 85px + tab-panel padding 15px），`.drag-title` 改为 `text-align: left` / `flex: 0 0 auto` / `font-weight: 400`（去加粗）。
- 标题从顶部居中移到左侧，标题栏仍可拖拽。改动尚未提交/发布。

## 发布 v1.1.5
- 设置窗口标题左移并与内容区对齐、去除加粗 + 版本号升 1.1.4 → 1.1.5，commit `bd64663` 已 push，tag `v1.1.5` 已 push。
- `cargo tauri build` 成功（1m30s，Rust 命中缓存，DMG 未假失败）。dmg 校验 VALID。
- 英文前缀资产 `RiXing_1.1.5_aarch64.dmg` / `.app.tar.gz` 已上传；Release https://github.com/zlimao/rixing/releases/tag/v1.1.5 （ad-hoc 签名）。

## 设置窗口 tab 改名
- 修改 `src/settings.html`：倒计时 tab（data-tab=ann）图标从定位 marker 换成日历图标（line + filled 两套），名称改为静态文本「日历」（去掉 data-i18n）；防休眠 tab（data-tab=keepawake）名称改为静态文本「不休」。
- 仅改 tab 上显示的字，面板内部「启用防休眠」「重要日期倒计时」等文案不受影响。改动尚未提交/发布（候选 v1.1.6）。

## 修复：设置窗口顶部标题栏仍显示旧名（2026-07-28）
- 问题：两个 `tab-panel` 用 `data-title-key="ann_section"` / `data-title-key="keepawake"` 驱动顶部 `.drag-title`（`updateWindowTitle()` 读 i18n），而这两个 i18n key 在面板内部大量用作「倒计时/防休眠」文案，不能全局改。
- 修复：
  - `settings.html`：两个面板改为静态 `data-title="日历"` / `data-title="不休"`（与侧边栏 tab 静态中文一致）。
  - `settings.js`：`updateWindowTitle()` 优先用 `dataset.title` 静态标题，回退到 i18n `data-title-key`。`switchTab()` 已调用该函数，切换即刷新。
- `ann_section` / `keepawake` i18n key 保持原值不变，面板内部文案不受影响。
- 此改动尚未提交/发布（候选 v1.1.6）。
