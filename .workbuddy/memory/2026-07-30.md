# 2026-07-30

## 打包 日行 v1.1.5（Tauri release build）
- 执行 `cargo tauri build`：`.app` 正常生成（`src-tauri/target/release/bundle/macos/日行.app`），但 dmg 阶段因 create-dmg 的 AppleScript 美化步骤无 Finder 自动化权限而失败（已知 -10004 问题）。
- 绕过：手动 `bundle_dmg.sh --skip-jenkins --volname 日行 --app-drop-link 200 120 --icon "日行.app" 100 120` 生成 dmg，功能完整（仅无自定义窗口布局）。
- 产物：
  - `bundle/dmg/日行_1.1.5_aarch64.dmg`（本地中文名，5.6MB）
  - `bundle/dmg/RiXing_1.1.5_aarch64.dmg`（GitHub 上传用英文名副本）
  - `bundle/dmg/RiXing_1.1.5_aarch64.app.tar.gz`（GitHub 上传用）
- 已验证 dmg 可挂载，内含 `日行.app` + `Applications` 软链。
- 注意：当前 HEAD 是 v1.1.5 tag 之后的 `bugfix` 提交，但 `tauri.conf.json` 版本仍为 1.1.5，未升版。若要发布此 bugfix，需先升到 1.1.6 以免覆盖已发布的 v1.1.5 release。
- 无代码签名证书，产物为 ad-hoc 签名，用户需右键打开或 `xattr -cr`。
