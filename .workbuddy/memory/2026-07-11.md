# 2026-07-11

## 设置面板 tab 重组 (src/settings.html, src/settings.js)
- 移除了「关于」独立 tab：删除 tabs 栏中的 about 按钮，以及独立的 `<div data-panel="about">` 面板。
- 将「关于」内容（版本号 + GitHub 链接）作为「关于」小节移动到「通用」页底部。
- tab 顺序调整为：翻译 → 轻听 → 倒计时 → 窗口分屏 → 通用（active）。即把「通用」tab 移到了「窗口分屏」之后。
- settings.js 的 `settings-open-tab` 事件监听中将后端发来的 `about` 映射为 `general`，保证托盘「关于」菜单仍能正确打开。
- initAbout() 无需改动，元素仍在 DOM 中（现在位于通用页内）。
